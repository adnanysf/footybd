package com.footybd.util;

public class ModelMaprapperUtils {
    // Utility methods for node operations
    public static boolean isNullOrEmpty(String str) {
        return str == null || str.trim().isEmpty();
    }
}